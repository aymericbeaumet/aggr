[package]
name = "aggr"
version = "1.9.0"
edition = "2024"
rust-version = "1.96"
license-file = "LICENSE"
description = "Git-native feed reader: aggr.toml in, static site out, history on a branch"
authors = ["Aymeric Beaumet <hi@aymericbeaumet.com>"]
repository = "https://github.com/aymericbeaumet/aggr"
readme = "readme.md"
keywords = ["rss", "atom", "feed", "reader", "static-site"]
categories = ["command-line-utilities", "web-programming"]

[dependencies]
ammonia = "4"
anyhow = "1"
base64 = "0.22"
bytes = "1"
chrono = { version = "0.4", features = ["serde"] }
clap = { version = "4", features = ["derive", "env"] }
clap_complete = "4"
comrak = { version = "0.54", default-features = false }
dom_smoothie = "0.18"
directories = "6"
encoding_rs = "0.8"
env_logger = "0.11"
feed-rs = "2"
futures-util = "0.3"
glob = "0.3"
hayro = { version = "0.7.1", features = ["embed-fonts"] }
hex = "0.4"
htmd = "0.5"
image = { version = "0.25", default-features = false, features = ["jpeg", "png", "gif", "webp", "bmp", "ico", "tiff", "qoi"] }
log = "0.4"
minijinja = { version = "2", features = ["loader", "json"] }
notify = "8"
pagefind = { version = "1.5.2", default-features = false }
quick-xml = "0.41"
regex = "1"
reqwest = { version = "0.13", default-features = false, features = [
  "rustls-no-provider",
  "gzip",
  "brotli",
  "json",
  "charset",
  "http2",
  "stream",
] }
rust-embed = { version = "8", features = ["debug-embed"] }
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12", "logging"] }
scraper = "0.27"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml_ng = "0.10"
sha1 = "0.11"
sha2 = "0.10"
slug = "0.1"
syntect = { version = "5", default-features = false, features = ["default-fancy"] }
tempfile = "3"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time", "net", "io-util", "signal"] }
toml = "1"
url = { version = "2", features = ["serde"] }
unicode-normalization = "0.1"
unicode-segmentation = "1"
walkdir = "2"
# These Apache-2.0 releases support the project's MSRV; newer releases require Rust 1.98.
wreq = { version = "=6.0.0-rc.31", default-features = false, features = ["webpki-roots", "tokio-rt", "gzip", "brotli", "stream"] }
wreq-util = { version = "=3.0.0-rc.14", default-features = false, features = ["emulation"] }
thumbhash = "0.1.0"
resvg = { version = "0.48.0", default-features = false, features = ["text"] }

[dev-dependencies]
assert_cmd = "2"
fantoccini = { version = "0.22", default-features = false }
httpmock = "0.8"
hyper-util = { version = "0.1", features = ["client-legacy", "http1", "tokio"] }
insta = "1"
png = "0.18"
predicates = "3"
tokio = { version = "1", features = ["test-util"] }

[profile.dev]
# Generic image decoders are instantiated in aggr itself, not just their dependency crates.
opt-level = 1
# Backtraces and profiles only need line numbers; full DWARF dominates the edit-compile-run loop.
debug = "line-tables-only"

[profile.dev.package."*"]
opt-level = 0

[profile.dev.package.fancy-regex]
opt-level = 3

[profile.dev.package.regex-automata]
opt-level = 3

# Asset validation hashes retained bytes even when their decoded metadata is cached.
[profile.dev.package.sha1]
opt-level = 3

[profile.dev.package.sha2]
opt-level = 3

# Keep image decoding, resizing and lossless encoding practical during local dev syncs.
[profile.dev.package.image]
opt-level = 3

[profile.dev.package.thumbhash]
opt-level = 3

[profile.dev.package.image-webp]
opt-level = 3

[profile.dev.package.png]
opt-level = 3

[profile.dev.package.fdeflate]
opt-level = 3

[profile.dev.package.zune-jpeg]
opt-level = 3

[profile.dev.package.moxcms]
opt-level = 3

[profile.release]
codegen-units = 1
lto = "fat"
opt-level = 3
panic = "abort"
strip = true
