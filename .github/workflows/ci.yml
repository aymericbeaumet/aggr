name: ci

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
  workflow_call:

env:
  CARGO_TERM_COLOR: always

jobs:
  frontend:
    name: frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci --prefix web
      - run: npm --prefix web run verify
      - name: Check embedded reader assets match their source
        run: git diff --exit-code -- themes/default/static/app.js themes/default/static/client.css themes/default/static/client.LICENSE

  test:
    name: test (${{ matrix.name }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - { name: linux-amd64, os: ubuntu-latest }
          - { name: linux-arm64, os: ubuntu-24.04-arm }
          - { name: macos-arm64, os: macos-latest }
          - { name: macos-amd64, os: macos-15-intel }
          - { name: windows-amd64, os: windows-latest }
          - { name: windows-arm64, os: windows-11-arm }
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Install native TLS build dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends cmake libclang-dev build-essential
      - name: Install native TLS build dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install cmake llvm
          echo "LIBCLANG_PATH=$(brew --prefix llvm)/lib" >> "$GITHUB_ENV"
      - name: Configure native Windows compiler environment
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: ${{ runner.arch == 'ARM64' && 'arm64' || 'x64' }}
      - name: Configure native TLS build dependencies (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $llvmBin = Join-Path $env:ProgramFiles 'LLVM\bin'
          if (!(Test-Path (Join-Path $llvmBin 'libclang.dll'))) {
            throw "The runner's native LLVM installation is missing libclang.dll"
          }
          "LIBCLANG_PATH=$llvmBin" >> $env:GITHUB_ENV
          $llvmBin >> $env:GITHUB_PATH
          'CMAKE_GENERATOR=Ninja' >> $env:GITHUB_ENV
          if ('${{ runner.arch }}' -eq 'ARM64') {
            'CC=clang-cl' >> $env:GITHUB_ENV
            'CXX=clang-cl' >> $env:GITHUB_ENV
            'ASM=clang-cl' >> $env:GITHUB_ENV
          } else {
            choco install nasm --yes --no-progress
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
            (Join-Path $env:ProgramFiles 'NASM') >> $env:GITHUB_PATH
          }
          cmake --version
          ninja --version
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      - run: cargo fmt --check
        if: matrix.name == 'linux-amd64'
      - run: cargo clippy --all-targets -- -D warnings
      - run: cargo test
      - name: Check shipped default config
        if: matrix.name == 'linux-amd64'
        run: cargo run --locked -- check --config config.default.toml

  browser:
    name: browser (chrome)
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    permissions:
      contents: read
    env:
      AGGR_WEBDRIVER_URL: http://127.0.0.1:9515
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Install native TLS build dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends cmake libclang-dev build-essential
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      - name: Install matching Chrome and ChromeDriver
        shell: bash
        run: |
          set -euo pipefail
          browser_dir="$(mktemp -d "$RUNNER_TEMP/aggr-chrome.XXXXXX")"
          curl --fail --silent --show-error --location --retry 3 \
            https://googlechromelabs.github.io/chrome-for-testing/last-known-good-versions-with-downloads.json \
            --output "$browser_dir/releases.json"
          for binary in chrome chromedriver; do
            download="$(jq -er --arg binary "$binary" \
              '.channels.Stable.downloads[$binary][] | select(.platform == "linux64") | .url' \
              "$browser_dir/releases.json")"
            curl --fail --silent --show-error --location --retry 3 \
              "$download" --output "$browser_dir/$binary.zip"
            unzip -q "$browser_dir/$binary.zip" -d "$browser_dir"
          done
          mapfile -t dependencies < "$browser_dir/chrome-linux64/deb.deps"
          sudo apt-get update -qq
          sudo apt-get satisfy -y --no-install-recommends "${dependencies[@]}"
          "$browser_dir/chrome-linux64/chrome" --version
          "$browser_dir/chromedriver-linux64/chromedriver" --version
          echo "AGGR_CHROME_BINARY=$browser_dir/chrome-linux64/chrome" >> "$GITHUB_ENV"
          echo "$browser_dir/chromedriver-linux64" >> "$GITHUB_PATH"
      - name: Test reader and offline browser contracts
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p target/browser-artifacts
          chromedriver --port=9515 --allowed-ips=127.0.0.1 \
            --log-path=target/browser-artifacts/chromedriver.log \
            > target/browser-artifacts/chromedriver-server.log 2>&1 &
          browser_driver_pid=$!
          trap 'kill "$browser_driver_pid" 2>/dev/null || true' EXIT
          for _attempt in {1..40}; do
            if curl --fail --silent "$AGGR_WEBDRIVER_URL/status" | jq -e '.value.ready == true' > /dev/null; then
              break
            fi
            kill -0 "$browser_driver_pid"
            sleep 0.25
          done
          curl --fail --silent --show-error "$AGGR_WEBDRIVER_URL/status" | jq -e '.value.ready == true'
          cargo test --locked --test browser -- --ignored --nocapture
      - name: Upload browser failure screenshots and logs
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: browser-chrome-failure
          path: target/browser-artifacts/
          if-no-files-found: ignore
          retention-days: 7
          compression-level: 0

  msrv:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - id: msrv
        run: echo "version=$(perl -ne 'print $1 if /^rust-version = "(.*)"/' Cargo.toml)" >> "$GITHUB_OUTPUT"
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ steps.msrv.outputs.version }}
      - name: Install native TLS build dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends cmake libclang-dev build-essential
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      - run: cargo build --locked

  # The version is committed in two places; keep them equal before anything gets tagged.
  version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          cargo="$(perl -ne 'print $1 if /^version = "(.*)"/' Cargo.toml)"
          file="$(tr -d '[:space:]' < VERSION)"
          [ "$cargo" = "$file" ] || { echo "Cargo.toml says $cargo, VERSION says $file"; exit 1; }

  # Exercise the same major-channel resolution used by the stable reusable workflow.
  installer:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - id: release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_REPOSITORY: aymericbeaumet/aggr
        run: |
          declared="$(tr -d '[:space:]' < VERSION)"
          major="${declared%%.*}"
          latest="$(gh release list \
            --repo "$RELEASE_REPOSITORY" \
            --exclude-drafts \
            --exclude-pre-releases \
            --limit 1000 \
            --json tagName \
            --jq "map(select(.tagName | test(\"^v${major}\\\\.[0-9]+\\\\.[0-9]+$\"))) | max_by(.tagName | ltrimstr(\"v\") | split(\".\") | map(tonumber)) | .tagName // empty")"
          [ -n "$latest" ] || { echo "no published v$major release"; exit 1; }
          echo "major=$major" >> "$GITHUB_OUTPUT"
          echo "expected=${latest#v}" >> "$GITHUB_OUTPUT"
      - id: install-major
        uses: ./
        with:
          version: ${{ steps.release.outputs.major }}
      - env:
          EXPECTED: ${{ steps.release.outputs.expected }}
        run: |
          actual="$(aggr --version | awk '{ print $2 }')"
          [ "$actual" = "$EXPECTED" ] || { echo "installed $actual, expected $EXPECTED"; exit 1; }
